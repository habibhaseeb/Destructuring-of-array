// Destructuring in JS is to extract values from array.
// Destructuring is commonly used in assignments,
// function parameters, and for iterating over arrays and objects. Now?
                    // first method
const [ John , petal , jims ] = [1, 2 , 3];

 console.log(John);

    console.log(petal);

        console.log(jims);
        
                    // Second method

  let obj = {Name:'john', age:'16'};

        let res = {class:'12', obj};

            console.log(res);
